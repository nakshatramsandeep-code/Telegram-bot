[project]
name = "telegram-bot"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "python-telegram-bot>=20.7",
    "anthropic>=0.34.0",
    "openai>=1.40.0",
    "google-genai>=1.0.0",
    "python-dotenv>=1.0.0",
]

[tool.vercel]
entrypoint = "api.webhook:handler"
